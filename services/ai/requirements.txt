# Runtime dependencies for the Hair AI service. Mirrors [project.dependencies]
# in pyproject.toml — keep the two in step.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
openai>=1.99.0,<2.0.0
pillow>=10.0.0
